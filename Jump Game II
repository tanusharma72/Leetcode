int jump(int* nums, int numsSize) {
    int jump=0;
    int i;
    int farthest=0;
    int current=0;
    for(i=0; i<numsSize-1; i++){
        farthest=fmax(farthest,nums[i]+i);
        if(i==current){
            current=farthest;
            jump++;
        }
    }
    return jump;
}
